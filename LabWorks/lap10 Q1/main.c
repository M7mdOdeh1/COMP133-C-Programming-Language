#include <stdio.h>
#include <stdlib.h>

typedef struct
    {
        char name[20];
        char author[30];
        int edition;
        int year;
        float price;
    }book_t;

int main()
{
    char Books[3];

    for(i=0;i<3;i++)

    scanf("%s",book_t.name);
    scanf("%s",boot_t.author);
    scanf("%d%d%lf",&edition,&year,&price);


    return 0;
}
