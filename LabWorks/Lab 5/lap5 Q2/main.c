#include <stdio.h>
#include <stdlib.h>

int main()
{int n=1,count=1;
while(count<=50)
{
    if((n%4)==0)
        {printf("%d is diviible by four\n",n);
        count++;
        }
n++;
}

    return 0;
}
